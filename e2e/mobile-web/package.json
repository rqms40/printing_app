{
  "name": "gridgo-mobile-web-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "cd ../../apps/mobile && fvm flutter build web --release --no-tree-shake-icons",
    "test": "playwright test",
    "test:beta:visual": "playwright test tests/beta-workflow-visual.spec.ts --project=beta-visual",
    "test:catalog:visual": "playwright test tests/catalog-rfq-visual.spec.ts --project=catalog-rfq-visual",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.57.0",
    "socket.io-client": "^4.8.3"
  }
}
