# v1.10 Integration Baseline

**Date:** 2026-08-10

**Branch:** `release/v1.10.0-catalog`

**Result:** histories integrated; pre-feature baseline recorded with known parent failures

## Revisions

- `origin/main` parent: `8ad8447de9e9073798d2070c52ae216e5612209e`
- local `GRIDGOv3` parent: `d06155c806183c1b3738e711d4ce7e38461e5af9`
- `origin/GRIDGOv3`: `a16f9ecce2055538aaa973930f72a470efa584ce`
- merge base: `b9d042527236d67de2534340b41a1bd1875370b4`
- merge commit: `151a5600d0cb563ed830f7d28b79f3a5b116d0e5`

`git rev-list --left-right --count origin/main...GRIDGOv3` returned `22 62`.
`git rev-list --left-right --count origin/GRIDGOv3...GRIDGOv3` returned
`0 2`; the two local-only commits are the approved v1.10 design
(`aea376d`) and plan (`d06155c`). The release worktree was clean at
`origin/main` before integration.

The integration used:

```text
git merge --no-ff GRIDGOv3 -m "merge: integrate GRIDGOv3 for release 1.10"
```

The resulting commit has both required parents. Contrary to the merge-tree
expectation in the task brief, Git reported two textual conflicts:

- `apps/mobile/lib/features/rider/shared/widgets/proof_of_delivery_sheet.dart`
- `server/src/chat/chat.gateway.ts`

The proof sheet resolution retains the GRIDGOv3 pickup/delivery OTP and photo
controls while preserving the v1.9.1 accessible close tooltip. The gateway
resolution retains GRIDGOv3's five-role mapping and admin-role helper while
preserving the v1.9.1 non-blocking rider-message notification fan-out.

## Dual-edited path review

All 19 required paths were reviewed with
`git diff $(git merge-base origin/main GRIDGOv3)..HEAD -- <path>` and, where
useful, against each parent. The merged result retains:

- v1.9.1 tutorial/account isolation, notification deep links, accessible
  modal barriers and icon labels, customer home/checkout behavior, rider map
  and proof accessibility, authoritative order refresh behavior, and rider
  chat acknowledgements/notifications;
- GRIDGOv3 client/supplier/ops/super role routing, supplier screens and access
  gates, catalog preview/payment authorization flows, marketplace order
  statuses, pickup/delivery OTP and proof controls, and marketplace module
  registrations.

No additional semantic conflict was found in the other 17 auto-merged paths.

## Merged baseline checks

### Server

```text
cd server && npm ci
```

Passed: 1,013 packages installed. Audit reported 43 known dependency
vulnerabilities (3 low, 19 moderate, 19 high, 2 critical).

```text
npm run lint:check
```

Failed: 74 findings (68 errors, 6 warnings). These are distributed across
marketplace audit, chat, credits, geo-zone, issue, matching, mockup, order,
payment, payout, product, quality, and super-admin code/tests.

```text
npm run build
```

Passed (exit 0).

```text
npm test -- --runInBand
```

Failed: 7 suites failed and 117 passed; 48 tests failed and 1,196 passed
(1,244 total). Failed suites were orders service, riders service, supplier
jobs, admin controller, chat gateway, auth service, and marketplace order
status transitions. Representative failures include stale address fixtures
without map pins, missing test providers after new service dependencies,
outdated transition expectations, and outdated constructor mocks.

### Admin

```text
cd admin && npm ci
```

Passed: 722 packages installed. Audit reported 22 known dependency
vulnerabilities (1 low, 7 moderate, 13 high, 1 critical).

```text
npx tsc --noEmit
```

Failed (exit 2): `src/pages/orders/show.tsx:74` declares unused
`hasCoordinates`.

```text
npm test
```

Failed: 4 files failed and 29 passed; 6 tests failed and 143 passed (149
total). Failures cover a stale supplier profile normalization expectation,
an ambiguous order-status text query, rider tests missing router context, and
stale login copy.

```text
npm run build
```

Failed (exit 2) on the same unused `hasCoordinates` TypeScript error.

### Mobile

```text
cd apps/mobile && fvm flutter pub get
```

Passed.

```text
fvm flutter analyze lib/
```

Failed: 13 findings (5 errors, 8 infos). All five errors are undefined
`AppColorSet.primary` references in order detail (3) and proof of delivery
(2); the remaining findings are lints.

```text
fvm flutter test
```

Failed after completing the full run: 626 passed and 27 failed. Failures
include the same undefined color getter, stale legacy `OrderStatus` enum
members in tests, an outdated registration test override, and client/customer
role expectation drift.

### Mobile web contracts

```text
cd e2e/mobile-web && npm ci
MOBILE_WEB_E2E_NO_SERVER=1 npm test -- tests/beta-workflow.spec.ts tests/marketplace-workflow.spec.ts
```

Passed: 26 passed, 2 opt-in live checks skipped (28 total). Audit reported one
known high-severity dependency vulnerability.

## Parent reproduction

The failing gates were reproduced independently in the clean `GRIDGOv3`
parent worktree at `d06155c806183c1b3738e711d4ce7e38461e5af9`:

- Server `npm run lint:check`: the same 68 errors and 6 warnings.
- Server targeted orders suite: 19 failed and 139 passed (158 total), including
  the same missing-map-pin and stale relation/history assertions.
- Admin `npx tsc --noEmit`: the same unused `hasCoordinates` error.
- Admin targeted user/order tests: 2 failed and 9 passed (11 total), matching
  the merged failures.
- Mobile `fvm flutter analyze lib/`: the same 5 errors and 8 infos.
- Mobile targeted proof sheet test: compilation failed on the same undefined
  `AppColorSet.primary` getter.

This evidence classifies the red server, admin, and mobile baseline as
pre-existing on `GRIDGOv3`, not introduced by the merge resolution. Production
code was therefore not changed merely to make these baseline gates green.

## Release concerns

- The release branch is integrated but not green outside the contract E2E
  suite. The parent failures above need dedicated cleanup before release.
- The dependency audit findings should be triaged separately; no automated
  dependency upgrade was attempted during this integration gate.
- The two manual conflict resolutions should remain covered by the existing
  proof-sheet and chat-gateway tests when the parent test debt is repaired.
