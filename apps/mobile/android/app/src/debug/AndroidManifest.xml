<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The debug variant alone permits the local Docker API over HTTP. -->
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
