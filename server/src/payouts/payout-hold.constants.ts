/** Hold reasons written on payouts during issue window / claims. */
export const PAYOUT_HOLD_ISSUE_WINDOW = 'issue_window';
export const PAYOUT_HOLD_OPEN_ISSUE = 'open_issue';
export const PAYOUT_HOLD_MANUAL = 'manual_hold';

/** Material issue window after delivery proof (PRD §6.3). */
export const ISSUE_WINDOW_MS = 24 * 60 * 60 * 1000;
